engines:
  duplication:
    enabled: true
    config:
      languages:
      - php
  fixme:
    enabled: true
  phpmd:
    enabled: false
  phpcodesniffer:
    enabled: true
    config:
      standard: "PHPCS"
ratings:
  paths:
  - "**.php"
exclude_paths:
- tests/*
